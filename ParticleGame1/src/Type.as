package
{
	
	/**
	 * ...
	 * @author Yelik
	 */
	public class Type
	{
		public var density:Number, color:uint, alpha:uint, falls:Boolean;
		
		public function Type(density:Number, color:uint, alpha:uint, falls:Boolean)
		{
			this.density = density;
			this.color = color;
			this.alpha = alpha;
			this.falls = falls;
		}
	}
}